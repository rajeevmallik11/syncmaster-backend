{
  "name": "syncmaster-backend",
  "version": "1.0.0",
  "description": "SyncMaster Backend Server",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "prisma generate && npx prisma db push && node src/index.js",
    "dev": "node --watch src/index.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/rate-limit": "^10.3.0",
    "@prisma/client": "^5.7.1",
    "fastify": "^5.8.4",
    "ioredis": "^5.3.2",
    "jose": "^5.2.0",
    "uuid": "^9.0.1",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3",
    "prisma": "^5.7.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
